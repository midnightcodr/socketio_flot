var config={
	interval:5,
	limit:1440, /* 2 hours worth of data for interval=5 seconds */
	LIMIT:8640,	/* 1 day worth of data for interval=5 seconds */
	zone_delta: -5	/* timezone for EST */
};

module.exports=config;
